# Oracle / 预知 — 0.16.3（修复 Boss 候选池顺序）

修复底注 6 等后期盲注偶发的 Boss 预测不一致：Steamodded 按未排序的哈希表顺序抽取候选 Boss，Oracle 原先在深拷贝状态中重新建表，可能改变候选顺序。现在只读获取当前原版候选数组，再用独立 RNG 模拟抽取，不推进真实随机流或修改牌局。条件分支变化后不会复用旧数组。真实不匹配时的保护继续保留。详见 [BOSS_POOL_ORDER.md](docs/BOSS_POOL_ORDER.md)。

## 0.16.2 幻觉开包验证时点

修正 0.16.1 对开包事件顺序的错误判断：幻觉先进行概率判定、预占栏位，但生成塔罗牌的事件被开包动画阻塞；包内卡牌先生成并进入补充包区域。现在分别记录「包内牌生成完成」和「动画及幻觉生成完成」两个 RNG 状态，开包验证使用前者，后者用于后续条件分支。避免把尚未发生的 RNG 调用提前拿来比较而误停全部预测。真实 mismatch 的停用保护仍保留。

已增加原版 Card.open、Card.explode 与 EventManager 的整体调度测试，覆盖 1／4／16／64 倍速度和不同帧步长。F10 日志导出、配置和撤销历史不变。安装后重启游戏生效。

## 0.16.1 幻觉开包预测与日志导出

增加持有「幻觉」时的补充包预测（含蓝图／头脑风暴复制、栏位预占、削弱与概率修正）；实际事件顺序已在 0.16.2 修正，商店补充包与标签赠送的包共用同一模拟流程。仍保留不支持规则和真实 mismatch 的保护。

**F10：导出诊断日志**，也可点击预知页底部「导出日志」。预测已停用、版本不匹配或没有当前牌局时也能导出。成功后点击「打开日志文件夹」，将新生成的 `Oracle-日期-时间.txt` 发给开发者。Windows 默认目录为 `%APPDATA%\Balatro\oracle-reports`，以游戏实际存档目录为准。报告仅保存到本机，包含牌局 seed／RNG、版本、预测状态、错误与日志片段，不包含完整存档、全局进度或撤销历史。详见 [PACK_OPENING_DIAGNOSTICS.md](docs/PACK_OPENING_DIAGNOSTICS.md)。

## 0.16.0 生成与目标类小丑悬浮预测

悬浮已持有的小丑即可查看下一次合格触发结果：卡牌术士、流浪者、第六感、通灵显示生成牌名；帕奇欧显示复制对象与负片结果；证书显示新扑克牌的点数、花色与蜡封。也覆盖乌合之众、超位置、大理石小丑、待办清单等现有适配器。偶像、古老小丑、邮件返利、城堡显示当前目标和下次目标，并根据盲注进度区分本底注／跨底注的目标刷新。预知小丑页面保留原版卡牌展示。

这些结果仍为 **Experimental 条件预测**：假设当前牌组、持牌与相关随机流不再改变，显示下一次满足条件的单独触发，不模拟整手牌中先执行的其他小丑或蓝图复制。悬浮只显示一次结果，概率事件深度设置保持不变。详见 [JOKER_HOVER.md](docs/JOKER_HOVER.md)。

## 0.15.2 修复连续使用消耗牌后的预测误停

修复愚者生成星球牌后立即使用该牌时，延迟验证把两次操作混在一起比较、误停全部预测的问题。验证现在会在下一次操作改变状态前完成；撤销会取消旧局的待验证回调，仍保留真实预测错误的停用保护。更新后重启游戏，解除旧会话的误停状态；配置、牌局存档与撤销历史保留。详见 [CONSUMABLE_BOUNDARY_FIX.md](docs/CONSUMABLE_BOUNDARY_FIX.md)。

## 0.15.1 修复补充包撤销崩溃

修复在打开补充包时撤销触发 `OPENED_BOOSTER` 为空的崩溃：原版重启使用异步事件，Oracle 现在先进入原版过渡状态，再清理旧补充包引用，避免下一帧访问已清空的对象。恢复期间连续按 F9 不再打开新面板。撤销历史格式不变，0.15.0 的历史可继续使用。详见 [UNDO_PACK_FIX.md](docs/UNDO_PACK_FIX.md)。

## 0.15.0 当前牌局撤销

新增 **F9 撤销**，或通过「预知 → 撤销」使用原版风格按钮。记录出牌、弃牌、购买、出售、使用消耗牌、开包、重掷商店／Boss、选择／跳过盲注、离开商店和兑现前的牌局状态。可以连续撤销；失败画面仍可按 F9，在开始新局前恢复。

只回退当前牌局的卡牌、金钱、盲注与 RNG；保留账号解锁、成就和生涯统计。撤销历史从安装后的操作开始，保存在独立文件中，正常退出后继续当前牌局仍可使用。补充包作为整体撤销，返回开包前；不单独回退包内某一次选牌。请等待动画和读档结束再撤销。

此功能标记 **Experimental**，原版序列化、卡牌读写和撤销调度已通过自动测试，完整游戏界面仍需实机验收。预测保持只读；只有主动执行撤销才改变当前牌局。现有左下角按钮、F8、悬浮预测与直接返回游戏保留，暂停菜单不增加入口。详见 [UNDO.md](docs/UNDO.md)。更新后重启游戏生效。

## 0.14.1 隐形小丑悬浮预测

鼠标悬浮自己持有的隐形小丑，在原版说明下显示「现在出售」的复制目标与生成牌名、版本和贴纸。复制目标若是负片，生成结果按原版去除负片。未达到所需回合、没有目标或没有可用栏位时显示对应原因。该快捷预测只显示一次结果，不受概率事件预测深度影响；出售前改变小丑或推进对应 RNG 后，重新悬浮会更新。商店和收藏中的隐形小丑不显示出售预测。沿用现有 shadow 复制适配器与原版验证，不增加新页面。

## 0.14.0 可调概率事件预测深度

在「设置」或「消耗牌／小丑触发」页面，通过原版选项控件调整「概率事件预测深度」：1／3／5／10／20 次，默认 5 次。独立于商店／底注预测深度。预知页每页显示 5 次，翻页可查看全部结果；命运之轮、小丑和幸运／玻璃牌的快捷悬浮始终只显示下一次有效判定。

支持命运之轮、太空小丑、大麦克、卡文迪什、血石、商业名片、预留泊车位、八号球、幻觉、错印，以及幸运牌和玻璃牌。命运之轮显示成功／失败、目标与版本；生成效果显示生成牌名；幸运牌分别显示倍率和金钱判定。

这是 **Experimental 条件序列**：以当前底注及概率修正连续执行同类有效判定，假设期间没有其他行动或同一 RNG 流的额外消耗。命运之轮假设每次已有一张可用牌，不模拟获取过程。保留前次版本变化与槽位占用；无目标、槽位满或命中销毁判定时停止，不虚构后续结果。详见 [EVENT_DEPTH.md](docs/EVENT_DEPTH.md)。更新后重启游戏生效，原定下一阶段仍暂停。

## 0.13.4 读档后卡牌身份碰撞修复

修复读档后新卡与已有卡牌复用原版 sort_id，导致消耗牌验证误报、连带停用标签／商店／补充包预测的问题。Oracle 现在用私有弱引用表区分实际卡牌对象，仅把数字身份放入快照，不在游戏卡牌上增加字段。原版 sort_id 继续用于 RNG 候选排序，真实错误保护保留。详见 [OBJECT_IDENTITY_FIX.md](docs/OBJECT_IDENTITY_FIX.md)。

## 0.13.3 开包后快捷预览恢复

修复预知页面可用，但补充包悬浮提示在开过包之后一直显示等待的问题。当前 Steamodded 保留上次 OPENED_BOOSTER 引用，Oracle 现在读取实际开包状态及面板生命周期，避免将历史引用误认为正在开包。开包、关闭动画及重掷过程中仍等待稳定状态；不会清空或改写游戏的补充包引用。详见 [PACK_HOVER_FIX.md](docs/PACK_HOVER_FIX.md)。

## 0.13.2 消耗牌验证误停修复

修复使用女祭司等消耗牌后，原版重新编号扑克牌导致验证器误判牌组变化，从而关闭全部预测的问题。使用稳定的卡牌身份对齐验证数据，保留实际牌序、重复牌、属性及 RNG 检查；不会修改真实牌组或关闭错误保护。详见 [CARD_IDENTITY_FIX.md](docs/CARD_IDENTITY_FIX.md)。

## 0.13.1 投资标签误拦截修复

修复蓝色牌组白注随机局中持有投资标签时，商店、重掷及补充包预测被错误关闭的问题。投资标签只参与击败 Boss 后的奖金结算，杂耍标签只参与盲注开始，两者与双倍标签共用经过原版回调核对的无关效果检查。不会移除标签、提前发钱或修改 RNG；改变商品的标签及未知自定义回调继续受保护。

## 0.13.0 悬浮快捷预览

新增三种局内原版悬浮提示，无需先打开预知面板：

- 商店补充包：保留原版说明，下方列出现在打开时的全部候选牌名，包括强化、版本、蜡封和 Soul → Legendary。
- 右下角牌堆：显示当前实际牌序的前 8 张；更多结果通过预知牌堆页查看。商店和盲注选择等即将洗牌的状态不把旧牌序当作下轮预测。
- 消耗牌：审判、女祭司、皇帝、命运之轮等直接显示生成牌或成功／失败、目标及版本。也复用已有星球／幻灵及确定性消耗牌模拟。商店牌按当前状态假设使用，提示不含购买效果；购买后再次悬浮会重新读取状态。

预测只在打开悬浮提示时读取快照；相同快照复用缓存。重新悬浮会检查 RNG、选牌、持有牌和牌组变化。关闭 Oracle 或对应功能设置后停用。保留钩子高亮、背面牌识别、左下角按钮、F8 和直接返回游戏。原定下一阶段仍暂停。

详见 [QUICK_PREVIEW_VALIDATION.md](docs/QUICK_PREVIEW_VALIDATION.md)。更新后重启游戏生效。

## 0.12.0 钩子目标与背面牌识别

按本次新指令添加两项局内功能，原定经济及后续阶段仍暂停。

- 遇到钩子 Boss，在手牌中选中准备打出的牌，剩余手牌中将被强制弃掉的最多两张会使用原版 voucher 闪光效果发亮。改选牌后即时同步，未选牌、Boss 失效或离开选牌阶段时取消高亮。不会给卡牌添加版本或修改属性。
- 鼠标悬浮背面朝上的牌，显示真实名称；包括琥珀橡果洗乱的小丑和 Boss 导致面朝下的手牌。扑克牌显示花色和点数，强化牌附加强化名称。保留牌背和原版玩法状态。
- 钩子预测先从 shadow 手牌移除已选牌，再按原版 sort_id 选池规则连续调用私有 hook RNG；实际出牌时自动比较强制弃牌实体与 hook 流。

详情及验证边界见 [CARD_INSIGHT_VALIDATION.md](docs/CARD_INSIGHT_VALIDATION.md)。局内左下角按钮、F8 和直接返回游戏的逻辑均保留；更新后重启游戏。

## 0.11.2 返回逻辑调整

预知的「返回」现在直接调用原版关闭面板流程：关闭预知、解除暂停并回到游戏，不再打开暂停菜单。保留局内左下角按钮和 F8。更新后重启游戏生效，下一阶段开发仍暂停。

## 0.11.1 入口调整

0.11.0 已实机验收。按要求暂停下一阶段开发，本次仅移除暂停菜单里的「预知」按钮；保留局内左下角按钮与 F8 快捷键。预测和设置不变，更新后重启游戏生效。

## 0.11.0 Boss 影响分析

0.10.0 已实机验收。本版完成扩展 Phase 4 的确定性 Boss 规则分析。

打开「预知」，切换到第三组「随机序列 / Boss 影响」，进入「Boss 影响」：

- 「规则」：显示当前 Boss，或本底注已排定 Boss 的原版说明，以及入场时的手牌上限、出牌或弃牌次数变化。
- 「受影响牌张」：按当前全牌组属性统计被 Boss 削弱的扑克牌，原版卡面、tooltip、每页四张。处理石头牌、万能牌、幻视、模糊小丑、本底注已打出牌和 Boss 失效。
- 「出牌条件」：选择真实可见牌型和张数，查看心灵、眼睛、嘴的限制，手臂的等级下降，公牛／牙齿的直接金钱效果，燧石对基础筹码和倍率的变化。
- 默认启用的预测核对会观察实际 Boss 卡牌削弱、牌型检查和基础分数变化，记录 MATCH；不匹配记录输入和 Expected / Actual，并关闭本次会话预测。

结果保留 **Experimental**。入场分析假设当前牌组、Joker 和状态保持不变；不是后续打法或整手得分预测。钩子、转轮、琥珀橡果、猩红之心、蔚蓝铃铛等随机目标，以及连续抽牌／翻面事件，本页明确只显示规则，尚不模拟其结果。当前 Boss 已付出的入场代价不会再扣一次。

详见 [BOSS_ANALYSIS_VALIDATION.md](docs/BOSS_ANALYSIS_VALIDATION.md)。下一阶段为经济分析。

## 0.10.0 消耗牌后的牌组模拟

0.9.0 已实机验收。本版完成扩展 Phase 3 的单次消耗牌、实体牌组与当前待抽牌堆模拟。

在「消耗牌」页面将「使用结果」切换为「使用后牌组」，可查看使用前／后、待抽牌／手牌／弃牌／全部牌，并使用原版卡面、tooltip、分页和列表。选牌变化后重新同步。

- Death、Cryptid 保留复制属性并区分实体牌；吊死鬼和火祭从 shadow 区域及总牌组删除实体。
- Strength、Sigil、Ouija、花色塔罗更新底层点数和花色；强化修改保留永久加成。
- Grim、Familiar、Incantation 的生成牌进入 shadow 手牌；后续待抽顺序从修改后的 shadow deck 读取。
- 预测使用私有 RNG，卡牌与区域数据均独立复制；实际使用后比较牌组属性及区域，检测牌堆顺序偏移。

**边界：** 显示一次用牌后的直接牌组状态，仍标注 Experimental。新生成的牌在手牌中；不会虚构其已进入待抽牌堆。补充包视图显示关包回收前的状态；关包后需重新同步。下一盲注洗牌、正向手牌上限变化触发的自动抽牌，以及后续出牌／弃牌／整手计分链不在本阶段内。

详见 [DECK_STATE_VALIDATION.md](docs/DECK_STATE_VALIDATION.md)。

## 0.9.0 小丑随机触发

0.8.2 已实机验收。本版完成扩展 Phase 2，新增第三组分页中的「小丑触发」。

- 自动读取当前持有的随机效果小丑，以及牌组中的幸运牌、玻璃牌；按当前状态预测下一次合格触发。
- 太空小丑、香蕉、卡文迪什、血石、名片、预留停车位、八号球、幻觉显示实际判定结果。幸运牌分别显示倍率与金钱，允许同时成功；玻璃牌显示碎裂判定。
- 显示隐形小丑、帕奇欧、疯狂、待办清单的随机目标，错版的倍率，以及偶像、邮件返利、古老小丑、城堡的下一次目标重置。
- 显示乌合之众、占卜师、第六感、流浪者、叠加态、通灵的生成牌；八号球和幻觉成功时继续显示塔罗；石头小丑和证书显示生成的扑克牌与蜡封。
- 原版卡牌、tooltip、按钮与选项循环，中英本地化；独立 shadow state / RNG，按需缓存。

所有新增结果标注 **Experimental / 下一次合格触发**。这不是整手计分或整个回合的事件链模拟：必须满足页面列出的条件，并且在之前没有其他动作、重复触发或复制效果推进同一 RNG。回合目标重置可以明确选择当前底注或下一底注，牌组保持当前状态。幸运猫和一路狂飙本身没有独立随机抽签。

详见 [JOKER_TRIGGER_VALIDATION.md](docs/JOKER_TRIGGER_VALIDATION.md)。

## 0.8.2 购买并使用验证修复

修复商店「购买并使用」消耗牌后，验证器误将购买花费计入使用效果、进而关闭后续标签预测的问题。
原版先排队扣款，再同步调用用牌；验证现在在独立快照中按扣款后的余额计算，真实 RNG 和游戏事件不变。
包括隐者和幽灵等依赖当前余额的效果；真正的不匹配仍会关闭预测，不自动清除保护状态。
升级后请完整退出并重新启动 Balatro，以加载修复并清除旧版本会话中的错误保护状态。

详见 [BUY_USE_VALIDATION.md](docs/BUY_USE_VALIDATION.md)。

## 0.8.1 标签奖励

0.8.0 已初步验收。本版在进入下一扩展阶段前补齐六种原版标签：

- 标准 / 吊饰 / 流星 / 小丑 / 空灵标签：悬浮显示跳过后开启的包及全部候选牌，包含版本、蜡封、强化、贴纸及 Soul → 传奇小丑。
- 充值标签：按当前小丑栏位、拥有牌和 RNG 显示实际生成的小丑；满栏明确显示不生成。
- 保留原版标签说明，在其下追加预知内容；标签链页提供原版卡牌预览。
- Double Tag 的充值副本继续顺序生成。多个补充包只确定当前第一个包，后续包明确等待选择；处理前包后按真实状态刷新。
- 免费标签开包与玩家主动用牌区分，避免验证器把自动开包误判成玩家改变分支。

详见 [TAG_REWARD_VALIDATION.md](docs/TAG_REWARD_VALIDATION.md)。新预测保留 Experimental。

## 0.8.0 消耗牌

0.7.0 已由玩家验收。本版新增「消耗牌」分页，位于第三组「随机序列 / 消耗牌」。
选择当前持有或正在打开的包中的 Tarot、Planet、Spectral，查看此刻使用的直接结果。
需要指定目标的牌，请先回到游戏选中手牌，再打开预知；没有合格目标时会明确提示。

- 覆盖 52 张原版消耗牌：生成、复制、销毁、点数/花色/强化/蜡封/版本变化、牌型等级和直接金钱变化。
- Wheel 显示成功/失败及实际目标版本；Soul、Judgement、Wraith 复用同一 Joker 生成模拟。
- Immolate 使用私有 Lua RNG 的原版洗牌调用顺序；Ankh 保留原版目标排序、永恒例外与负片复制规则。
- 预览采用原版 Card / CardArea / tooltip；中英本地化，按当前选择缓存，无每帧完整模拟。
- 新增 prediction/engine.lua 统一调度入口及使用后效果、RNG 的自动核对。

本阶段显示 **Experimental 直接效果**。Joker 连锁触发、完整后继牌组/抽牌分支、
Boss 分析、经济、What-if 与 Timeline 按新路线的后续阶段推进；本版不声称已完成这些功能。
测试方法、覆盖范围与实机检查记录见 [CONSUMABLE_VALIDATION.md](docs/CONSUMABLE_VALIDATION.md)。

## 0.7.0 回归加固

0.6.0 已由玩家实机验收通过。本阶段补齐异常路径和可重放的多 seed 回归：

- 无效 RNG 数据在创建私有 Lua 状态前拒绝；模拟执行或结果导出失败后显式释放状态。
- 商店验证包装器完整保留原函数返回值，包括中间和末尾的 nil；原函数仍只执行一次。
- 增加异常注入、验证日志写入失败、零商店栏位、过渡状态缓存恢复与多 key RNG 对照测试。
- `--stress` 扩展原版函数对照；`--seed-start` 选择批次起点；`--report` 输出耗时、源码 SHA-256 与失败上下文。

详细覆盖范围及限制见 docs/PHASE7_VALIDATION.md。界面沿用已验收的 Phase 6；
通过抽样回归不代表任意行动链或未知模组均可准确预测，现有 Experimental 标记保留。

## Phase 6 功能

0.5.4 已由玩家实机验收通过。本版补齐设置、紧凑分页导航和高级 RNG 信息。

- 原版 tabs 每组最多四页，使用原版选项切换器换组，避免横向九页撑大窗口。
- 设置：启用、自动刷新、显示抽牌顺序、未来商店、补充包、优惠券、
  高级信息、实际验证；预测深度 3 / 5 / 10 / 20。所有控件使用原版组件。
- 关闭未来商店仍显示当前实际商品；关闭自动刷新后可手动刷新或换页。
- 新增随机序列页：开启高级信息后，分页查看当前真实 per-key 状态，不推进 RNG。
- 高级卡牌 tooltip 显示所在底注、重掷次数或补充包、位置；默认隐藏技术字段。
- 使用独立 configuration.lua 补齐旧配置，保留玩家开关和深度偏好。

详见 docs/PHASE6_VALIDATION.md。预测准确性边界沿用已验收版本。

0.5.4 同时修复商店入口：持有原版 Double Tag 时可以查看当前商店及连续
重掷预测。商店直接读取当前栏位数，兼容两种库存过剩；购买后按实际商品和
持有状态更新。0.5.3 的补充包修复保留。详见 docs/SHOP_DOUBLE_FIX.md。

修复 CZZXRBWB 报告的问题：浮雕牌组获得 Double Tag 后，补充包页不再被
商店的「持有任何标签就禁止预测」规则误拦截。已审计的九种原版标签使用
独立开包资格检查；未知标签或自定义回调仍明确拒绝。无需重开该局，更新后
重启 Balatro 即可使用。详见 docs/PACK_TAG_FIX.md。

Phase 1–5 及 0.5.1–0.5.4 已由玩家实机验收通过。

## 0.5.2 新增

- Double Tag 按原版顺序消耗并构造独立标签实例。两个 Double 加 Rare 会逐次
  生成三个结果，保留每一步的 RNG、候选池、版本和贴纸。
- Orbital 副本继承同一已选牌型，逐次累加当前等级；副本升级数量来自原版
  Tag 构造器，不把被修改过的原标签 config 整份复制过去。
- Voucher Tag 区分正常商店券与额外券。每张额外券生成后更新后续候选池；
  生成券不视为购买，不会提前解锁加强版。空池遵循原版 Blank 回退。
- 新增原版风格「标签链」分页，显示顺序、真实卡牌、版本及延迟结算结果。
  原版 tooltip 和底注摘要保留；持有的 Double 显示当前数量和等待复制说明。
- 初始商店放不下的标签继续保留，并在设定深度内模拟无购买的连续重掷。
  商店栏位直接读取实际 joker_max，包含两种库存过剩的影响。
- 自动核对新增实例、执行顺序、各实例结果以及正常/额外券序列。
  对照方法、结果和边界见 [TAG_CHAIN_VALIDATION.md](docs/TAG_CHAIN_VALIDATION.md)。

## Skip Tag 新功能

- Orbital：读取已生成的实际牌型、当前等级及实际标签的升级数量。
  原版选择池使用当前可见牌型；秘密牌型没有自动全部加入。
- Uncommon / Rare：使用当前候选池与 `uta` / `rta` RNG 流生成 Joker。
- Foil / Holographic / Polychrome / Negative：找出初始商店中实际能被修改的
  无版本 Joker，保留其永恒、易腐、租用属性。
- 原版 Skip Tag tooltip 下方增加预知说明；当前底注页显示摘要与真实 Joker
  预览卡牌，版本使用原版 shader。tooltip 本身保留原版说明，以文字补充预测。
- 标签预测在打开 tooltip / 页面时按完整状态签名缓存，没有每帧 RNG 模拟。
- 跳过前保存 Expected；结算后核对 Actual。中途条件改变记录 CONDITION_CHANGED，
  相同条件下偏差记录 MISMATCH。完整诊断见 `oracle-tag-validation.log`。

Joker 标签在后续商店才结算，因此全部标为 Experimental 条件预测。分支假设
跳过 Small 后打 Big、跳过 Big 后打 Boss，期间相关 RNG 流、池和持有卡牌不变。
Big 分支使用下一 Ante。它不模拟尚未进行的出牌、改牌、第二次跳过或其他效果。
达到设定重掷深度仍没有合格 Joker 时显示标签保留，不臆测无限次重掷后的目标。
未适配标签组合、自定义可见性/候选池回调明确显示无法可靠预测。
未来底注的结果需到达后读取真实状态，不能沿用当前底注的候选池作确定结论。
详见 docs/TAG_VALIDATION.md。

## 使用

将 Oracle 文件夹放入 `%APPDATA%/Balatro/Mods` 后重启游戏。
入口：暂停 → 预知；局内「选项」下方的「预知」按钮；F8。

- 总览：自动读取 seed、Ante、Blind、Stake、Deck。
- 牌堆：待抽顺序、手牌、弃牌、全部扑克牌；卡牌 / 列表视图，每页 8 张。
  直接读取现有扑克牌，保留点数、花色、强化、版本、蜡封和永久加成。
- 商店：当前商品与连续重掷结果。购买、出售或用券后自动以最新状态计算。
  栏位读取 `G.GAME.shop.joker_max`；库存过剩及其加强版分别增加一栏。
  当前商品已经买走的位置保持空缺；重掷按完整栏位数生成。
- 补充包：当前商店中的五类补充包候选牌；Soul → 传奇小丑；黑洞。
  已打开的包显示真实生成候选牌。未知扩展规则会明确显示不可预测。
- 底注：当前已生成信息，以及后续 Tag / Boss / Voucher 条件预测。
- 重掷 Boss：从当前 RNG 和已使用 Boss 计数开始的连续重掷分支。
- 设置：启用、自动刷新、抽牌顺序、未来商店、补充包、优惠券、高级信息、实际结果验证；预测深度 3 / 5 / 10 / 20。

使用原版 UIBox、CardArea、Card sprite/shader、原版卡牌描述、tabs、字体和
`G.C.*` 颜色。兼容 English / 简体中文。面板不会默认遮挡游戏。

## 预测边界

商店、补充包和跨底注预测仍明确标记 Experimental；它们是当前状态下的
条件分支，不是所有玩家行动都会得到的唯一未来。

- 连续重掷分支假设中间不再购买、出售、使用卡牌。执行这些操作后会重新取样，
  不是继续沿用购买前的序列。
- 库存过剩会即时补齐商店缺少的商品并消耗 RNG；预测从这一步完成后的真实
  RNG 继续。不会把券的标志再加到已有栏位数上，从而重复计算。
- 每个未开补充包都按「从现在先打开该包」计算；不把两个独立分支拼在一起。
  Soul 分支指全部候选牌生成后，未进行其他选择/使用而立即使用 Soul。
- 持有 Hallucination、非原版池回调/自定义 Soul 等尚无独立适配时，关闭相关
  预测。首个 Buffoon Pack 的非 keyed 随机封面只显示已知类型，注明封面未知。
- 牌序是 `G.deck.cards` 的末尾到开头。它能反映此前实际发生的抽牌、弃牌、
  出牌、复制、销毁、创建、Tarot/Spectral 修改及洗牌结果。不会推演玩家未来
  尚未执行的改牌动作。新盲注会洗牌，因此商店中的当前堆叠不能当作下一盲注牌序。
- 抽牌页显示顺序，不承诺一次抽多少张；手牌容量和 The Serpent 等决定抽取数量。
- 精确开发环境：Balatro 1.0.1o-FULL、Steamodded 26.829.0、Lovely 0.9.0、
  Windows 游戏自带 LuaJIT 2.0.5。未知版本保留只读信息并关闭未验证的预测。

## 隔离与同步

模拟数据和 per-key RNG 深拷贝。调用游戏 lua51.dll 建立独立 Lua 全局状态，
仅在该状态运行随机数；异常路径同样释放。没有真实 RNG 执行后回滚，没有
替换 G.GAME，也不构造会推进 RNG 的真实游戏 Card 来预览。

UI-only Card 保留原版渲染，跳过游戏 Card 构造器、set_ability 与 Card.remove。
描述函数克隆为新的 Lua 函数对象；原函数及环境不变，隔离概率上下文与
Misprint 随机文字。高级信息关闭时不显示内部 key。

购买、出售、抽牌、牌张修改、洗牌和 change_shop_size 等事件使缓存失效。
打开/换页会读新状态。已打开面板只检查一个 revision 整数，变化时通过原版
事件队列合并刷新；没有每帧完整 RNG 预测或牌堆扫描。预览卡牌不会触发游戏
缓存失效循环。

## 验证

自然触发游戏生成时，包装器先在副本预测，再执行原函数一次并核对。
商店、Boss、Tag、Voucher、补充包及 Soul 核对结果和完整 keyed RNG 状态。
抽牌核对实际牌张 ID、花色、点数、强化、版本和蜡封；抽牌时原版翻面随机行为
可能正常推进 RNG，因此仅记录前后 RNG，不错误地要求其不变。

MISMATCH 会关闭本次进程的未来预测，记录 seed、Ante、round、输入快照、
相关池/RNG 轨迹和 Expected / Actual 到 LÖVE 存档目录 `oracle-validation.log`。
日志两文件轮转，内存最多保留最近 16 条核对。

开发测试（玩家无需 Python）：

```powershell
python tests/run.py --game-dir 'D:/steam/steamapps/common/Balatro' --stress `
  --patched-dir "$env:APPDATA/Balatro/Mods/lovely/dump" `
  --smods-dir '<当前 Steamodded 源码目录>' --report oracle-regression.json
```

真值函数从本机实际 Lovely dump 和 Steamodded 提取到临时目录，测试后删除；
发行包不包含 Balatro 源码。详见 docs/PHASE7_VALIDATION.md。
`--phase7` 使用较小的默认样本；`--fast` 会跳过 Phase 2 的长测试，完整验收请不加该参数。
